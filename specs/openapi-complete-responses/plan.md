---
feature: openapi-complete-responses
derived_from: spec.md
---

# Plan — OpenAPI complete responses

1. Spec + INDEX entry.
2. Auditar cada servicio (controllers → use-cases → mapper) y anotar status reales.
3. Reescribir `docs/api/openapi.json` con componentes de error compartidos + ejemplos.
4. Sync `apps/web/public/openapi.json`.
5. Living docs; format/lint; commit.

## Tasks

Ver `tasks.md`.
