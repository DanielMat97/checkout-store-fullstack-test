# Tasks — payment-gateway

- [x] **PG1** — Definir `PaymentGatewayPort` + tipos de error dominio
- [x] **PG2** — Adapter sandbox HTTP (env neutro) + tests con mock HTTP
- [x] **PG3** — Fake adapter in-memory (APPROVED/DECLINED configurable)
- [x] **PG4** — Wiring Nest module transactions (provider binding)
- [x] **PG5** — Checklist: cero brand name en `services/**` y `packages/**`

Pre-flight: living-docs; no keys reales en git.

Docs: `docs/payment-adapter.md`. Local default: `PAYMENT_GATEWAY_MODE=fake`.
