# Tasks — payment-gateway

- [ ] **PG1** — Definir `PaymentGatewayPort` + tipos de error dominio
- [ ] **PG2** — Adapter sandbox HTTP (env neutro) + tests con mock HTTP
- [ ] **PG3** — Fake adapter in-memory (APPROVED/DECLINED configurable)
- [ ] **PG4** — Wiring Nest module transactions (provider binding)
- [ ] **PG5** — Checklist: cero brand name en `services/**` y `packages/**`

Pre-flight: living-docs; no keys reales en git.
