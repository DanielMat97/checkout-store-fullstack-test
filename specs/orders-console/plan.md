# Plan — orders-console

1. Persistence: list txs by GSI, incrementStock, delivery CANCELLED, getByTransaction.
2. Use-cases: ListTransactions, RestoreOrder, MarkDeliveryFulfilled.
3. HTTP controllers + OpenAPI.
4. FE `/orders` + API client + ShellHeader link.
5. Stock poll helper on Status/Product after APPROVED.
6. Tests.
